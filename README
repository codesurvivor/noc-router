Author: Chao Chen, chao.chen@polymtl.ca

This project implements NoC static and dynamic routers. Static router is an XY router. Dynamic router is an Odd-Even router and a DyAD router.

booksim2.0 is used for high level simulation.

Verilog is used as the HDL language.
